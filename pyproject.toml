[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "cython",
    "ivpm-build",
    "ivpm",            # still needed at build time for PkgInfoRgy queries
]
build-backend = "setuptools.build_meta"
