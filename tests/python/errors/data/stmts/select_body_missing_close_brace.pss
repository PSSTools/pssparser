//! id:     PSS021
//! at:     11:20
//! match:  unclosed '{' for select statement
//! count:  1
//! xfail:  E9-S9-D1 -- D8's opener-pointing fix (E-6) is scoped to Component_declarationContext/Struct_declarationContext only; an unclosed 'select { ...' body still misattributes the EOF mismatch to the enclosing component instead of naming the select statement
component C {
    action A {
    }
    action B {
        activity {
            select {
                A;
        }
    }
}
