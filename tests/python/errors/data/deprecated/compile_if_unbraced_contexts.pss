//! id:     PSS104
//! at:     10:9-28
//! match:  without enclosing braces is deprecated
//! also:   9:5 "'compile if' begins here"
//! severity: warning
//! count:  3
const int X = 1;
package P {
    compile if (X == 1)
        struct S1 { int a; }
}
component C {
    compile if (X == 1)
        action A { int a; }
}
struct S2 {
    compile if (X == 1)
        int f;
}
